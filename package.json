{
  "name": "ATMOS",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "node -e \"const fs = require('fs'); fs.existsSync('package-lock.json') && fs.unlinkSync('package-lock.json'); fs.existsSync('yarn.lock') && fs.unlinkSync('yarn.lock'); if(!process.env.npm_config_user_agent?.includes('pnpm')) { console.error('Use pnpm instead'); process.exit(1); }\"",
    "dev:backend": "pnpm --filter @workspace/api-server run dev",
    "dev:frontend": "pnpm --filter @workspace/mockup-sandbox run dev",
    "dev:mobile": "pnpm --filter @workspace/mobile run dev",
    "dev:web+api": "pnpm -r --parallel --filter @workspace/api-server --filter @workspace/mockup-sandbox run dev",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "build:vercel": "pnpm run typecheck:libs && pnpm --filter @workspace/api-server run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "devDependencies": {
    "@types/node": "^25.6.2",
    "expo": "^55.0.23",
    "prettier": "^3.8.1",
    "typescript": "~5.9.2"
  }
}
